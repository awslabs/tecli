use: version [options]
short: Displays the version of tfe-cli and all installed plugins
long: |
  Displays the version of Terraform and all installed plugins in the following format:
  tfe-cli/{git-tag} {golang-version} {architecture} {source}